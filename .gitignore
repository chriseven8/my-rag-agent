__pycache__/
*.pyc
.pytest_cache/

# 虚拟环境
.venv/

# 密钥与本地配置:绝不提交(.env 里有真实 OPENAI_API_KEY)
.env

# 运行时数据:上传的原文件 + SQLite 索引
data/

# 编辑器
.idea/
.claude/

# 前端依赖
frontend/node_modules/

# 注意:frontend/dist 是**故意提交**的。
# 这样 clone 下来不装 Node 也能直接起页面(后端直接托管静态产物)。
# 改过 frontend/src 之后,记得先 npm run build 再提交,否则提交的是旧页面。
