# 版本已锁定,保证 clone 下来装到的就是作者验证过能跑通的一套。
# 未锁定的 langchain 生态变动很快,不锁容易出现「装完跑不起来」。
# 开发环境:Python 3.13.12(代码要求 >= 3.10)

# --- Web 框架 ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32

# --- LLM / 向量库 ---
# langchain 全家桶版本互相咬合,一起锁;langchain-core 与 text-splitters
# 虽然只是间接依赖,但被代码直接 import,显式列出避免被动漂移。
langchain==1.3.18
langchain-core==1.6.1
langchain-openai==1.6.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
pgvector==0.5.0
psycopg2-binary==2.9.12

# --- 文档解析 ---
pypdf==6.16.2
python-docx==1.2.0
openpyxl==3.1.5

# --- 检索 / 配置 ---
jieba==0.42.1
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- 测试 ---
pytest==9.1.1
httpx==0.28.1
